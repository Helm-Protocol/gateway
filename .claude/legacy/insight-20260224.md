# Global Legacy Insights
# 생성: 2026-02-24
# Origin: qkvg-gateway-mindmap-v1.md

---

## [20260224] WindowUpdateMode API 변경 (libp2p-yamux 0.45+)

Origin: qkvg-gateway-mindmap-v1.md, Day 0
Pattern: `WindowUpdateMode::OnRead` 컴파일 에러 → `WindowUpdateMode::on_read()` 메서드로 변경됨
Application: libp2p 0.53+ 사용하는 모든 Rust P2P 프로젝트
Evidence:
```
error[E0599]: no associated item named `OnRead` found for struct `WindowUpdateMode`
Fix: cfg.set_window_update_mode(WindowUpdateMode::on_read());
```

---

## [20260224] SHA2 의존성 위치 오류 패턴

Origin: qkvg-gateway-mindmap-v1.md, Proof of Novelty 구현
Pattern: Cargo.toml 하단 echo 명령으로 추가 시 [profile.release] 섹션 밑에 들어감 → 컴파일 에러
Application: 모든 Rust 프로젝트 Cargo.toml 의존성 추가 시
Evidence:
```
# 잘못된 위치 (profile.release 하단)
[profile.release]
opt-level = 3
sha2 = "0.10"  ← 여기 들어가면 에러

# 올바른 위치 ([dependencies] 섹션)
[dependencies]
sha3 = "0.10"
sha2 = "0.10"  ← 여기
```
Fix: python3로 직접 파일 파싱 후 올바른 위치에 삽입

---

## [20260224] G-Metric Goldilocks Zone 과금 공식

Origin: qkvg-gateway-mindmap-v1.md, Day 2
Pattern: 정보 참신성을 수학적으로 측정 → 동적 과금
Application: 지식 중개 플랫폼, RAG 시스템, 정보 필터링 서비스
Evidence:
```
G = 1.0 - max{ cos(Q, Kᵢ) : Kᵢ ∈ K }

G < 0.10  → 중복 → Base Toll만 (0.0001 BNKR)
G 0.10~0.80 → Goldilocks → 0.01 + (G-0.10) × 0.10 BNKR
G > 0.80  → 스팸 → 드롭

Knowledge SNR = G / (1-G)  ← SFE물리층 SNR과 동형
```
Revenue: 캐시 히트 시 마진 100% (원가 $0)

---

## [20260224] Kaleidoscope SafeStream 보안 패턴

Origin: qkvg-gateway-mindmap-v1.md, Day 0
Pattern: 모든 외부 스트림을 래퍼로 감싸 메모리/시간 공격 차단
Application: 모든 Rust 비동기 네트워크 서버
Evidence:
```rust
KaleidoscopePolicy {
    max_payload_bytes: 2MB,       // OOM 공격 차단
    read_timeout_secs: 3,         // Slowloris 차단
    min_bytes_per_sec: 1024,      // 느린 연결 차단
    first_byte_timeout_secs: 5,   // 고스트 커넥션 제거
}
// yamux: max_streams=32, window=128KB, on_read backpressure
```

---

## [20260224] TOCTOU 방어 패턴 (DID + 잔고 동시 검증)

Origin: qkvg-gateway-mindmap-v1.md, H-6/H-7 수정
Pattern: 검증 후 실행 사이 시간차 공격 방어
Application: 모든 인증 + 결제가 결합된 시스템
Evidence:
```sql
-- WRONG: 두 번의 쿼리 사이 공격 가능
SELECT balance FROM visas WHERE did = ?;  -- check
UPDATE visas SET balance = balance - ? WHERE did = ?;  -- use

-- RIGHT: 원자적 트랜잭션
BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE;
SELECT balance FROM local_visas WHERE local_did = $1 FOR UPDATE;
-- 이 시점부터 해당 행 잠금
UPDATE local_visas SET balance = balance - $2 WHERE local_did = $1;
COMMIT;
```

---

## [20260224] x402 State Channel 마이크로결제 패턴

Origin: qkvg-gateway-mindmap-v1.md, Day 1
Pattern: 오프체인 잔고 관리 + 주간 L1 롤업
Application: 가스비 0원이 필요한 모든 Web3 결제 시스템
Evidence:
```
에이전트 호출 100회 → 오프체인 잔고만 차감 (가스비 $0)
주간 1회 → Merkle root를 L1 컨트랙트에 제출 (가스비 1건)
절감: 1/100 ~ 1/1000 가스비
```

---

## [20260224] Proof of Novelty 투명성 패턴

Origin: qkvg-gateway-mindmap-v1.md, Grand Cross 최종
Pattern: API 응답에 수학 증명서 첨부 → 신뢰 → 브랜드
Application: 유료 정보 API, 데이터 품질이 가격에 영향을 주는 서비스
Evidence:
```http
X-G-Score: 0.720
X-Reference-K: a3f91c8b4e2d1...  (SHA-256 검증 가능)
X-Novelty-Proof: 기존 K에 없던 신규 벡터 감지
X-Computation-Hash: b7e3a9f12c...  (독립 재현 가능)
```
효과: 블랙박스 → 투명한 오라클 → 에이전트 신뢰 → 생태계 락인

---

_마지막 업데이트: 2026-02-24_
