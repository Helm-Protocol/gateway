# Helm Protocol — Claude Code 작업 규칙

## 자동 트리거: 워크플로우 다이어그램

**사용자가 "워크플로우" 키워드를 언급하면 다음을 자동 실행:**

1. `/home/user/Helm/워크플로우 다이어그램.md` 파일을 열어 최신 취약점 현황 확인
2. 변경된 소스 파일 스캔 (`crates/helm-node/src/gateway/**`)
3. 취약점 패치 현황 업데이트 (완료된 항목 체크)
4. 새 취약점 발견 시 목록에 추가
5. 업데이트된 파일을 사용자에게 렌더링해서 보여주기

---

## 🔄 메인 개발 워크플로우 (필수 순서)

```
📝 [1. 구현]
─────────────────────────────────────────────────────
 • 기능 코드 작성 (Rust / Axum)
 • 단위 모듈별 로직 완성
 • 의존성 체크

        │
        ▼

🧪 [2. 시뮬레이션 테스트] ← Claude가 직접 실행
─────────────────────────────────────────────────────
 • cargo test --workspace (전체 워크스페이스)
 • 점-다 (1 에이전트 : 다수 작업)
 • 점-점 (에이전트 A ↔ B 상호작용)
 • 다-다 (여러 에이전트 × 여러 작업)
 • 공격 시나리오 (계속 새로운 것 발견 시도):
     - DID 스푸핑 / 서명 위조
     - 오버플로 / 잔고 부정
     - OOM / 메모리 폭탄
     - Rate Limit bypass
     - SQL Injection / Log Injection
     - 자기참조 루프
     - Reentrancy 공격
     - 슬로우로리스 공격
     - 0값 / 경계값 / NaN / Inf
     - 미래 상황 새로운 공격 시도 계속 찾아서 보완

        │
        ▼

📊 [3. 보고 (Jay에게)] ← 커밋 전 필수
─────────────────────────────────────────────────────
 발견된 취약점 전체 목록:
 ┌──────────────────────────────────────────────────┐
 │  🔴 CRITICAL  — 즉시 수정 필요 (배포 차단)       │
 │  🟠 HIGH      — 이번 커밋 전 수정 권장           │
 │  🟡 MEDIUM    — 다음 이터레이션 수정 가능        │
 │  🟢 LOW/INFO  — 개선 아이디어 / 최적화 여지      │
 └──────────────────────────────────────────────────┘
 + 개선 가능 요소 (성능·보안·코드품질) 목록
 + 워크플로우 다이어그램.md 업데이트

        │
        ▼

✅ [4. Jay 승인]
─────────────────────────────────────────────────────
 • Jay가 보고 검토
 • 수정 지시 or 승인
 • 승인 없이는 다음 단계 진행 불가

        │
   ┌────┴─────────────────────┐
   │ 수정 지시                 │ 승인
   ▼                           ▼
🔁 [1번으로 돌아가]       🚀 [5. 커밋 & 푸시]
                           ─────────────────────
                           git commit (보안: 요약)
                           git push origin claude/...
                           (태그 / 버전 업데이트)
```

---

## 보안 패치 원칙

- **커밋 전 반드시 `cargo test --workspace`로 시뮬레이션 테스트**
- 에러-0%, 미검증 코드 커밋 절대 금지
- CRITICAL 취약점은 즉시 패치, HIGH는 이번 커밋 전, MEDIUM/LOW는 배치 처리
- 패치 시 반드시 `워크플로우 다이어그램.md`의 해당 항목을 ✅로 업데이트
- **Jay 승인 없이는 커밋/푸시 불가**

## 개발 브랜치

- 항상 `claude/update-github-oauth-strategy-wT1JP` 브랜치에서 작업
- 커밋 메시지: `security: <패치 내용 요약>`

## 배포 필수 환경변수

```
HELM_ADMIN_SECRET=<64바이트 랜덤 hex>
HELM_CORS_ORIGINS=https://your-frontend.com
HELM_PORT=8080
```
